namespace apiTests.Controller;

// [TestFixture]
public class HealthCheckControllerTests
{
    // [Test]
    // public void GetVersion_ShouldReturn_ExpectedApiVersion()
    // {
    //     // Given
    //     var controller = GetTarget();
    //
    //     // When
    //     var result = controller.GetVersion();
    //
    //     // Then
    //     Assert.Multiple(() =>
    //     {
    //         Assert.That(result, Is.Not.Null);
    //         Assert.That(result.Value.IsSuccess, Is.True);
    //         Assert.That(result.Value.Content.Version, Is.EqualTo("0.0.2"));
    //         Assert.That(result.Value.Content.CreatingTime <= DateTime.Now && result.Value.Content.CreatingTime >= DateTime.Now.AddSeconds(-1), Is.True);
    //     });
    // }
    //
    // private HealthCheckController GetTarget() => new();
}
